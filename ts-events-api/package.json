{
  "name": "ts-events-api",
  "private": true,
  "scripts": {
    "build": "esbuild src/cell.ts --bundle --outfile=dist/cell.js --format=esm"
  },
  "devDependencies": {
    "esbuild": "^0.25.0"
  }
}
